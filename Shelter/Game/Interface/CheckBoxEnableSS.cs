using UnityEngine;

// ReSharper disable once CheckNamespace
public class CheckBoxEnableSS : MonoBehaviour
{
//    private bool init; (Unity field)

    private void Awake()
    {
        Destroy(this);
    }
}

