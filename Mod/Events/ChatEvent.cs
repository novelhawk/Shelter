using System;

namespace Mod.Events
{
    public class ChatEvent
    {
        //TODO: Handle #{id} and links (both from send [bit.ly them] and receive [Open the link? !yes !no // Clickable chat])
        public ChatEvent()
        {
            throw new NotImplementedException();
        }
    }
}
